{
  "name": "GraphQL Schema",
  "schema": "client-app/core/api/graphql/schema.json",
  "extensions": {
    "endpoints": {
      "GraphQL Endpoint": {
        "url": "${env:APP_BACKEND_URL}/xapi/graphql",
        "headers": {
          "user-agent": "JS GraphQL"
        },
        "introspect": false
      }
    }
  },
  "include": ["client-app/**/*.gql", "client-app/**/*.graphql", "client-app/**/*.ts", "client-app/**/*.vue"],
  "exclude": ["node_modules"]
}
