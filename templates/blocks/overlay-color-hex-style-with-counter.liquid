{% assign colorHex = predefinedColor %}
{% if customColor != '' %}
    {% assign colorHex = customColor %}
{% endif %}
{% assign hexCounter = hexCounter | plus: 1 %}
<style>
    #{{block.__id}} > .block:after
    {
        background: {{colorHex}};
        opacity:.7;
        bottom: 0;
	    content: ' ';
	    left: 0;
	    position: absolute;
	    right: 0;
	    top: 0;
	    z-index: 5;
    }
</style>
