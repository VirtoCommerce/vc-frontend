# v3.200.36
name: Release

on:
  workflow_dispatch:

jobs:
  release:
    uses: VirtoCommerce/.github/.github/workflows/release.yml@VCST-4492 #v3.800.35
    secrets:
      envPAT: ${{ secrets.REPO_TOKEN }}
