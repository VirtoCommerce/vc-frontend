// TODO: Sync color config, tailwding colors and this enum
export enum Colors {
  primary = "primary",
  secondary = "secondary",
  neutral = "neutral",
  info = "info",
  success = "success",
  warning = "warning",
  danger = "danger",
  additional = "additional",
}

export type ColorType = `${Colors}`;
