#import "./cartId.graphql"
#import "./shortLineItem.graphql"

fragment shortCart on CartType {
  ...cartId
  itemsQuantity
  items {
    ...shortLineItem
  }
  shipments {
    id
  }
  validationErrors {
    ...validationError
  }
}
