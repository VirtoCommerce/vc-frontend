#import "../../common/fragments/money.graphql"

fragment shortLineItem on LineItemType {
  id
  sku
  quantity
  productId
  extendedPrice {
    ...money
  }
  configurationItems {
    id
    sectionId
    type
    productId
    customText
    files {
      url
    }
  }
}
