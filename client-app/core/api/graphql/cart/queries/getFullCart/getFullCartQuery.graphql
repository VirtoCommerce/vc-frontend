#import "../../fragments/fullCart.graphql"

query GetFullCart(
  $storeId: String!
  $userId: String!
  $currencyCode: String!
  $cultureName: String
  $cartId: String
  $cartType: String
  $cartName: String
) {
  cart(
    storeId: $storeId
    userId: $userId
    currencyCode: $currencyCode
    cultureName: $cultureName
    cartId: $cartId
    cartType: $cartType
    cartName: $cartName
  ) {
    ...fullCart
  }
}
