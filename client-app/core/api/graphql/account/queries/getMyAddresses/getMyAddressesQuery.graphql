#import "../../../fragments/memberAddressFields.graphql"

query getMyAddresses($sort: String) {
  me {
    contact {
      addresses(sort: $sort) {
        items {
          ...memberAddressFields
        }
      }
    }
  }
}
