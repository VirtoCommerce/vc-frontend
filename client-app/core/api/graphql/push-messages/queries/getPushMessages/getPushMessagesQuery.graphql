#import "../../fragments/pushMessage.graphql"

query GetPushMessages($unreadOnly: Boolean, $cultureName: String) {
  pushMessages(unreadOnly: $unreadOnly, cultureName: $cultureName) {
    unreadCount
    items {
      ...pushMessage
    }
  }
}
