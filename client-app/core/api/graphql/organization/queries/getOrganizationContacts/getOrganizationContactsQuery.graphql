query GetOrganizationContacts(
  $id: String!
  $userId: String!
  $first: Int
  $after: String
  $sort: String
  $searchPhrase: String
  $roleIds: [String]
) {
  organization(id: $id, userId: $userId) {
    contacts(first: $first, after: $after, sort: $sort, searchPhrase: $searchPhrase, roleIds: $roleIds) {
      items {
        id
        name
        firstName
        lastName
        fullName
        emails
        status
        isLockedInOrganization(organizationId: $id)
        rolesInOrganization(organizationId: $id) {
          id
          name
        }
        securityAccounts {
          id
          email
          roles {
            id
            name
          }
        }
      }
      totalCount
    }
  }
}
