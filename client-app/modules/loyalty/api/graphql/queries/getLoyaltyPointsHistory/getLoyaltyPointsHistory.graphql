query GetLoyaltyPointsHistory($sort: String, $after: String, $first: Int, $operationType: String, $storeId: String!) {
  loyaltyPointsHistory(sort: $sort, after: $after, first: $first, operationType: $operationType, storeId: $storeId) {
    totalCount
    items {
      id
      object {
        type
        orderId
        orderNumber
      }
      operationType
      amount
      createdDate
    }
  }
}
