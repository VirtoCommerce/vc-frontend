mutation ShareListWithCustomers($command: InputShareListWithCustomersType!) {
  shareListWithCustomers(command: $command) {
    succeeded
    listId
    sharingKey
    sharingUrl
    sharedWithOrganizationIds
    warnings
  }
}
