mutation SaveSalesRepLayout($command: InputSalesRepLayout!) {
  saveSalesRepLayout(command: $command) {
    regions {
      id
      blocks {
        id
        type
        hidden
        settings {
          key
          value
        }
      }
    }
  }
}
