query SalesRepDocument($id: String!) {
  salesRepDocument(id: $id) {
    id
    name
    displayName
    category
    isPinned
    contentType
    size
    createdDate
    modifiedDate
    url
    summary
    pageCount
    previewUrl
  }
}
