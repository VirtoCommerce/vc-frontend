query SalesRepCustomer($organizationId: String!) {
  salesRepCustomer(organizationId: $organizationId) {
    organizationId
    organizationName
    accountType
    phone
    address {
      city
      regionName
    }
    primaryContact {
      id
      fullName
      name
    }
  }
}
