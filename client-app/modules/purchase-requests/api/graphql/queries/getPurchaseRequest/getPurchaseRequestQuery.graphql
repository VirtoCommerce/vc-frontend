query GetPurchaseRequest($purchaseRequestId: String!) {
  purchaseRequest(purchaseRequestId: $purchaseRequestId) {
    id
    number
    cartId
    quoteId
    sources {
      name
      url
      contentType
      size
    }
  }
}
