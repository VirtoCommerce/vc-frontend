mutation SubmitReturn($command: SubmitReturnCommandType!) {
  submitReturn(command: $command) {
    id
    number
    status
    availableActions {
      name
      isAvailable
      unavailableReason
    }
  }
}
