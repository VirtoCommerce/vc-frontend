mutation CancelReturn($command: CancelReturnCommandType!) {
  cancelReturn(command: $command) {
    id
    number
    status
    availableActions {
      name
      isAvailable
      unavailableReason
    }
    cancelReason
  }
}
