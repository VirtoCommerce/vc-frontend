mutation CancelReturn($command: CancelReturnCommandType!, $cultureName: String) {
  cancelReturn(command: $command) {
    id
    number
    status
    statusDisplayValue(cultureName: $cultureName)
    availableActions {
      name
      isAvailable
      unavailableReason
    }
    cancelReason
  }
}
