mutation AddBulkItemsCart($command: InputAddBulkItemsType!) {
  addBulkItemsCart(command: $command) {
    errors {
      objectId
      errorCode
      errorParameters {
        key
        value
      }
    }
  }
}
