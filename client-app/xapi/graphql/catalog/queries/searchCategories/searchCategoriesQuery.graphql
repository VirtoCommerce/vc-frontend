query Categories(
  $storeId: String!
  $userId: String!
  $currencyCode: String!
  $cultureName: String
  $after: String
  $first: Int
  $filter: String
  $sort: String
) {
  categories(
    storeId: $storeId
    userId: $userId
    after: $after
    first: $first
    currencyCode: $currencyCode
    cultureName: $cultureName
    filter: $filter
    sort: $sort
  ) {
    items {
      id
      name
      code
      slug
      outline
      parent {
        id
      }
      seoInfo {
        pageTitle
        metaKeywords
        metaDescription
        semanticUrl
      }
      breadcrumbs {
        title
        seoPath
      }
    }
    totalCount
  }
}
