{
  "name": "Pages",
  "previewUrl": "",
  "request": [
    {
      "url": "/api/pagebuilder/search?storeId=B2B-store&theme={{config.themeName}}&type=pages",
      "method": "get",
      "cacheable": true,
      "response": {
        "selector": "Object.keys(this.response).map(x => ({ ...this.item, ...this.response[x], request: undefined })).reduce((acc, x) => ({ ...acc, [x.name]: x }), {})",
        "isArray": true
      }
    }
  ],
  "type": "pages",
  "sections": [
    "call-to-action",
    "call-to-action-with-image",
    "features",
    "image",
    "page-header",
    "products",
    "text",
    "title"
  ],
  "settings": [
    {
      "id": "header",
      "label": "Page Header / H1",
      "type": "string"
    }
  ]
}
