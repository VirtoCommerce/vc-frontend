schema: https://st-platform.dev.govirto.com/graphql
documents: "./client-app/**/*.(graphql|gql|ts)"
generates:
  ./client-app/core/api/graphql/types.ts:
    config:
      skipTypename: true
    plugins:
      - typescript
      - typescript-operations
